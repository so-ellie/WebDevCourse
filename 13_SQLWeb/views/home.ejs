<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Home</title>

    <style>
      :root {
        --bg0: #0b0611;
        --bg1: #12051a;
        --pink: #ff4fd8;
        --pink2: #ff86ea;
        --lilac: #b58cff;
        --text: #f7e9ff;
        --muted: rgba(247, 233, 255, 0.72);
        --shadow: 0 18px 60px rgba(0, 0, 0, 0.55);
        --radius: 18px;
      }
      * {
        box-sizing: border-box;
      }
      html,
      body {
        height: 100%;
      }
      body {
        margin: 0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto,
          Arial;
        color: var(--text);
        background: radial-gradient(
            1100px 760px at 12% 18%,
            rgba(255, 79, 216, 0.18),
            transparent 55%
          ),
          radial-gradient(
            900px 680px at 82% 28%,
            rgba(181, 140, 255, 0.18),
            transparent 60%
          ),
          radial-gradient(
            700px 520px at 40% 95%,
            rgba(255, 134, 234, 0.1),
            transparent 60%
          ),
          linear-gradient(180deg, var(--bg0), var(--bg1));
        overflow-x: hidden;
        display: grid;
        place-items: center;
        padding: 28px 14px;
      }

      .halo {
        position: fixed;
        inset: -40% -40%;
        pointer-events: none;
        background: radial-gradient(
            circle at 20% 30%,
            rgba(255, 79, 216, 0.1),
            transparent 55%
          ),
          radial-gradient(
            circle at 70% 60%,
            rgba(181, 140, 255, 0.1),
            transparent 60%
          );
        filter: blur(10px);
        animation: breathe 9s ease-in-out infinite;
      }
      @keyframes breathe {
        0%,
        100% {
          transform: scale(1);
          opacity: 0.9;
        }
        50% {
          transform: scale(1.03);
          opacity: 1;
        }
      }

      .shell {
        width: min(760px, 94vw);
        padding: 1px;
        border-radius: var(--radius);
        background: conic-gradient(
          from 180deg,
          rgba(255, 79, 216, 0.9),
          rgba(181, 140, 255, 0.7),
          rgba(255, 134, 234, 0.8),
          rgba(255, 79, 216, 0.9)
        );
        box-shadow: var(--shadow);
        animation: appear 0.6s ease-out both;
      }
      @keyframes appear {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: none;
        }
      }

      .card {
        border-radius: calc(var(--radius) - 1px);
        padding: 26px 22px;
        background: linear-gradient(
          180deg,
          rgba(20, 10, 31, 0.92),
          rgba(16, 7, 26, 0.92)
        );
        border: 1px solid rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(10px);
      }

      .top {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 14px;
        margin-bottom: 18px;
      }

      .badge {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 10px;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(255, 255, 255, 0.06);
        color: var(--muted);
        font-size: 12px;
      }
      .spark {
        width: 10px;
        height: 10px;
        border-radius: 999px;
        background: radial-gradient(
          circle at 30% 30%,
          var(--pink2),
          var(--pink)
        );
        box-shadow: 0 0 0 6px rgba(255, 79, 216, 0.12);
        animation: pulse 2.6s ease-in-out infinite;
      }
      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
          box-shadow: 0 0 0 6px rgba(255, 79, 216, 0.12);
        }
        50% {
          transform: scale(1.08);
          box-shadow: 0 0 0 10px rgba(181, 140, 255, 0.1);
        }
      }

      h2 {
        margin: 0 0 6px;
        font-size: 26px;
      }
      .sub {
        margin: 0;
        color: var(--muted);
        line-height: 1.4;
      }

      .grid {
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        gap: 14px;
        margin-top: 18px;
      }
      @media (max-width: 720px) {
        .grid {
          grid-template-columns: 1fr;
        }
      }

      .panel {
        border: 1px solid rgba(255, 255, 255, 0.08);
        background: rgba(255, 255, 255, 0.05);
        border-radius: 16px;
        padding: 14px;
        transition: transform 0.12s ease, box-shadow 0.18s ease;
      }
      .panel:hover {
        transform: translateY(-1px);
        box-shadow: 0 14px 36px rgba(0, 0, 0, 0.25);
      }

      .who {
        font-size: 14px;
        color: var(--muted);
      }
      .who b {
        color: var(--text);
        font-size: 16px;
      }

      .btn {
        width: 100%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        padding: 12px 14px;
        border: none;
        border-radius: 14px;
        cursor: pointer;
        color: #140a1f;
        font-weight: 700;
        letter-spacing: 0.2px;
        background: linear-gradient(135deg, var(--pink), var(--lilac));
        box-shadow: 0 14px 30px rgba(255, 79, 216, 0.2);
        transition: transform 0.12s ease, box-shadow 0.18s ease;
      }
      .btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 18px 40px rgba(181, 140, 255, 0.22);
      }
      .btn:active {
        transform: translateY(0);
      }

      .btn.secondary {
        background: rgba(255, 255, 255, 0.06);
        color: var(--text);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: none;
      }

      .mini {
        font-size: 12px;
        color: var(--muted);
        margin-top: 10px;
      }

      .wave {
        margin-top: 10px;
        height: 10px;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
        overflow: hidden;
        border: 1px solid rgba(255, 255, 255, 0.08);
      }
      .wave > div {
        height: 100%;
        width: 40%;
        background: linear-gradient(
          90deg,
          rgba(255, 79, 216, 0.75),
          rgba(181, 140, 255, 0.75)
        );
        border-radius: 999px;
        animation: slide 2.6s ease-in-out infinite;
      }
      @keyframes slide {
        0% {
          transform: translateX(-10%);
        }
        50% {
          transform: translateX(120%);
        }
        100% {
          transform: translateX(-10%);
        }
      }

      @media (prefers-reduced-motion: reduce) {
        * {
          animation: none !important;
          transition: none !important;
        }
      }
    </style>
  </head>

  <body>
    <div class="halo"></div>

    <div class="shell">
      <div class="card">
        <div class="top">
          <div>
            <h2>Home</h2>
            <p class="sub">You’re logged in — everything is sparkling ✨</p>
          </div>

          <div class="badge">
            <span class="spark"></span>
            <span>Session active</span>
          </div>
        </div>

        <div class="grid">
          <div class="panel">
            <div class="who">
              Welcome, <b><%= user.fullName %></b><br />
              <span style="opacity: 0.95">(<%= user.email %>)</span>
            </div>

            <div class="mini">Quick status</div>
            <div class="wave" aria-hidden="true"><div></div></div>
            <div class="mini">
              Tip: your session will persist until you logout.
            </div>
          </div>

          <div class="panel">
            <div style="font-weight: 700; margin-bottom: 8px">Actions</div>

            <form method="POST" action="/logout" style="margin: 0">
              <button class="btn" type="submit">Logout</button>
            </form>

            <div style="height: 10px"></div>

            <a class="btn secondary" href="/login" style="text-decoration: none"
              >Go to Login</a
            >
            <div class="mini">Logout first if you want to switch accounts.</div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
