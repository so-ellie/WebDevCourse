<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "Auth App" %></title>

    <style>
      :root {
        --bg0: #0b0611;
        --bg1: #12051a;
        --card: #140a1f;
        --card2: #10071a;

        --pink: #ff4fd8;
        --pink2: #ff86ea;
        --lilac: #b58cff;
        --lilac2: #d5b6ff;

        --text: #f7e9ff;
        --muted: rgba(247, 233, 255, 0.72);
        --line: rgba(255, 255, 255, 0.1);

        --shadow: 0 18px 60px rgba(0, 0, 0, 0.55);
        --radius: 18px;
      }

      * {
        box-sizing: border-box;
      }
      html,
      body {
        height: 100%;
      }

      body {
        margin: 0;
        font-family:
          ui-sans-serif,
          system-ui,
          -apple-system,
          Segoe UI,
          Roboto,
          Arial,
          "Apple Color Emoji",
          "Segoe UI Emoji";
        color: var(--text);
        background:
          radial-gradient(
            1200px 800px at 10% 20%,
            rgba(255, 79, 216, 0.2),
            transparent 55%
          ),
          radial-gradient(
            900px 700px at 85% 30%,
            rgba(181, 140, 255, 0.18),
            transparent 60%
          ),
          radial-gradient(
            700px 500px at 40% 95%,
            rgba(255, 134, 234, 0.12),
            transparent 60%
          ),
          linear-gradient(180deg, var(--bg0), var(--bg1));
        overflow-x: hidden;
      }

      /* floating blobs */
      .blob {
        position: fixed;
        inset: auto;
        width: 420px;
        height: 420px;
        border-radius: 999px;
        filter: blur(38px);
        opacity: 0.55;
        pointer-events: none;
        transform: translate3d(0, 0, 0);
        animation: floaty 12s ease-in-out infinite;
      }
      .blob.one {
        top: -120px;
        left: -120px;
        background: radial-gradient(
          circle at 30% 30%,
          rgba(255, 79, 216, 0.85),
          rgba(255, 79, 216, 0)
        );
        animation-delay: -2s;
      }
      .blob.two {
        top: 8%;
        right: -160px;
        width: 520px;
        height: 520px;
        background: radial-gradient(
          circle at 40% 35%,
          rgba(181, 140, 255, 0.85),
          rgba(181, 140, 255, 0)
        );
        animation-delay: -6s;
      }
      .blob.three {
        bottom: -180px;
        left: 18%;
        width: 560px;
        height: 560px;
        background: radial-gradient(
          circle at 40% 35%,
          rgba(255, 134, 234, 0.65),
          rgba(255, 134, 234, 0)
        );
        animation-delay: -9s;
      }

      @keyframes floaty {
        0%,
        100% {
          transform: translate(0, 0) scale(1);
        }
        50% {
          transform: translate(18px, -22px) scale(1.03);
        }
      }

      /* sparkle grain */
      .grain {
        position: fixed;
        inset: 0;
        pointer-events: none;
        opacity: 0.12;
        mix-blend-mode: overlay;
        background-image:
          radial-gradient(
            circle at 20% 30%,
            rgba(255, 255, 255, 0.35) 0 1px,
            transparent 2px
          ),
          radial-gradient(
            circle at 70% 60%,
            rgba(255, 255, 255, 0.28) 0 1px,
            transparent 2px
          ),
          radial-gradient(
            circle at 45% 80%,
            rgba(255, 255, 255, 0.22) 0 1px,
            transparent 2px
          );
        background-size: 240px 240px;
        animation: drift 10s linear infinite;
      }
      @keyframes drift {
        from {
          transform: translate3d(0, 0, 0);
        }
        to {
          transform: translate3d(-60px, 40px, 0);
        }
      }

      .wrap {
        min-height: 100%;
        display: grid;
        place-items: center;
        padding: 28px 14px;
      }

      /* card with animated border */
      .cardShell {
        width: min(720px, 92vw);
        border-radius: var(--radius);
        position: relative;
        padding: 1px;
        background: conic-gradient(
          from 180deg at 50% 50%,
          rgba(255, 79, 216, 0.85),
          rgba(181, 140, 255, 0.7),
          rgba(255, 134, 234, 0.75),
          rgba(255, 79, 216, 0.85)
        );
        box-shadow: var(--shadow);
        animation: hue 10s linear infinite;
      }
      @keyframes hue {
        0% {
          filter: hue-rotate(0deg);
        }
        100% {
          filter: hue-rotate(18deg);
        }
      }

      .card {
        background: linear-gradient(
          180deg,
          rgba(20, 10, 31, 0.92),
          rgba(16, 7, 26, 0.92)
        );
        border-radius: calc(var(--radius) - 1px);
        border: 1px solid rgba(255, 255, 255, 0.08);
        padding: 26px 22px;
        backdrop-filter: blur(10px);
      }

      .headerRow {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        margin-bottom: 16px;
      }
      .brand {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      .logoDot {
        width: 12px;
        height: 12px;
        border-radius: 999px;
        background: radial-gradient(
          circle at 30% 30%,
          var(--pink2),
          var(--pink)
        );
        box-shadow: 0 0 0 6px rgba(255, 79, 216, 0.12);
        animation: pulse 2.6s ease-in-out infinite;
      }
      @keyframes pulse {
        0%,
        100% {
          transform: scale(1);
          box-shadow: 0 0 0 6px rgba(255, 79, 216, 0.12);
        }
        50% {
          transform: scale(1.08);
          box-shadow: 0 0 0 10px rgba(181, 140, 255, 0.1);
        }
      }
      .brand h1 {
        font-size: 16px;
        margin: 0;
        letter-spacing: 0.4px;
      }
      .brand p {
        margin: 0;
        color: var(--muted);
        font-size: 12px;
      }

      .contentIn {
        animation: appear 0.6s ease-out both;
      }
      @keyframes appear {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* shared UI bits */
      .title {
        margin: 10px 0 6px;
        font-size: 26px;
        letter-spacing: 0.2px;
      }
      .sub {
        margin: 0 0 16px;
        color: var(--muted);
        line-height: 1.4;
      }

      .error {
        margin: 10px 0 14px;
        padding: 10px 12px;
        border-radius: 12px;
        border: 1px solid rgba(255, 79, 216, 0.3);
        background: rgba(255, 79, 216, 0.1);
        color: #ffe6fb;
        animation: shake 0.42s ease-in-out;
      }
      @keyframes shake {
        0%,
        100% {
          transform: translateX(0);
        }
        20% {
          transform: translateX(-6px);
        }
        40% {
          transform: translateX(6px);
        }
        60% {
          transform: translateX(-4px);
        }
        80% {
          transform: translateX(4px);
        }
      }

      .field {
        margin: 12px 0;
      }
      label {
        display: block;
        font-size: 12px;
        color: var(--muted);
        margin: 0 0 6px;
      }
      .control {
        position: relative;
      }
      input {
        width: 100%;
        padding: 12px 12px;
        border-radius: 14px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(0, 0, 0, 0.18);
        color: var(--text);
        outline: none;
        transition:
          transform 0.12s ease,
          border-color 0.18s ease,
          box-shadow 0.18s ease,
          background 0.18s ease;
      }
      input::placeholder {
        color: rgba(247, 233, 255, 0.48);
      }
      input:focus {
        border-color: rgba(255, 79, 216, 0.5);
        box-shadow:
          0 0 0 6px rgba(255, 79, 216, 0.12),
          0 0 0 1px rgba(181, 140, 255, 0.1) inset;
        background: rgba(0, 0, 0, 0.22);
      }

      .hintRow {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        margin-top: 10px;
      }

      a {
        color: rgba(255, 134, 234, 0.95);
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }

      .btn {
        width: 100%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        padding: 12px 14px;
        border: none;
        border-radius: 14px;
        cursor: pointer;
        color: #140a1f;
        font-weight: 700;
        letter-spacing: 0.2px;
        background: linear-gradient(135deg, var(--pink), var(--lilac));
        box-shadow: 0 14px 30px rgba(255, 79, 216, 0.2);
        transition:
          transform 0.12s ease,
          box-shadow 0.18s ease,
          filter 0.18s ease;
        position: relative;
        overflow: hidden;
      }
      .btn:before {
        content: "";
        position: absolute;
        inset: -40% -60%;
        background: radial-gradient(
          circle at 20% 20%,
          rgba(255, 255, 255, 0.35),
          transparent 55%
        );
        transform: translateX(-30%) rotate(12deg);
        opacity: 0.7;
        transition: transform 0.6s ease;
      }
      .btn:hover {
        transform: translateY(-1px);
        box-shadow: 0 18px 40px rgba(181, 140, 255, 0.22);
      }
      .btn:hover:before {
        transform: translateX(25%) rotate(12deg);
      }
      .btn:active {
        transform: translateY(0);
        filter: saturate(1.05);
      }

      .btn.secondary {
        background: rgba(255, 255, 255, 0.06);
        color: var(--text);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: none;
      }
      .btn.secondary:hover {
        box-shadow: 0 12px 26px rgba(0, 0, 0, 0.22);
      }

      .mini {
        font-size: 12px;
        color: var(--muted);
      }

      .row {
        display: flex;
        gap: 10px;
        align-items: center;
        justify-content: space-between;
        margin-top: 14px;
        border-top: 1px solid rgba(255, 255, 255, 0.08);
        padding-top: 14px;
      }

      .spinner {
        width: 16px;
        height: 16px;
        border-radius: 999px;
        border: 2px solid rgba(20, 10, 31, 0.3);
        border-top-color: rgba(20, 10, 31, 0.92);
        animation: spin 0.8s linear infinite;
      }
      @keyframes spin {
        to {
          transform: rotate(360deg);
        }
      }

      @media (prefers-reduced-motion: reduce) {
        * {
          animation: none !important;
          transition: none !important;
        }
        .grain {
          display: none;
        }
      }
    </style>
  </head>

  <body>
    <div class="blob one"></div>
    <div class="blob two"></div>
    <div class="blob three"></div>
    <div class="grain"></div>

    <div class="wrap">
      <div class="cardShell contentIn">
        <div class="card">
          <div class="headerRow">
            <div class="brand">
              <div class="logoDot"></div>
              <div>
                <h1>Pink Lilac Auth</h1>
                <p>Secure login • smooth vibes</p>
              </div>
            </div>
            <div class="mini"><%= new Date().getFullYear() %></div>
          </div>

          <%- body %>
        </div>
      </div>
    </div>
  </body>
</html>
